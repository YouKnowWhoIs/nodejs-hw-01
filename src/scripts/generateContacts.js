import path from 'node:path';

import { PATH_DB } from '../constants/contacts.js';

const generateContacts = async (number) => {
  // const createFakeContact = path.join(process.cwd());
};

await generateContacts(5);
